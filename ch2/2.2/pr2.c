#include<stdio.h>
#include<conio.h>
main()
{
	int l,w;
	clrscr();
	printf("enter the value of length:");
	scanf("%d",&l);
	printf("enter the value of width:");
	scanf("%d",&w);
	printf("area of rectangle:%d",l*w);
	getch();
}